## 🧱 L'Établi du Raffineur : Anatomie du DataFrame

::: {.card .card-info}
::: {.card-header}
💡 Concept Clé : Au-delà du tableau Excel
:::
::: {.card-body}
Un DataFrame n'est pas qu'un simple tableau visuel. Pour le raffineur de données, c'est un agencement complexe en mémoire vive (RAM) dont la structure physique dicte la vitesse de vos calculs. Passer d'un code "qui marche" à un code industriel demande de comprendre comment ces données sont stockées [@markdown_2026_cadrestrategique].
:::
:::
### 🏗️ L'Anatomie du DataFrame

Un DataFrame moderne est composé de trois briques fondamentales qui collaborent pour optimiser l'accès aux données [@pandas_2026_docs] :

1. **L'Index (La Colonne Vertébrale) :** Structure de hachage optimisée. Il permet de passer d'une recherche lente (parcours complet) à une recherche instantanée en $O(1)$.
2. **Les Séries (Series) :** Chaque colonne est une `Series`. En science des données, on considère la colonne comme l'unité atomique de calcul.
3. **Le Backend (Le Moteur) :** Gère réellement les octets en RAM. Historiquement NumPy, aujourd'hui **Apache Arrow** pour plus de performance.

### ⚡ La Révolution Columnar (Stockage Colonnaire)

La grande différence entre une base de données classique (SQL) et un DataFrame réside dans l'organisation des données en mémoire.

* **Stockage par lignes (Transactionnel) :** Idéal pour modifier un utilisateur précis.
* **Stockage par colonnes (Analytique/IA) :** Idéal pour calculer la moyenne d'un million de prix.

::: {.card .card-success}
::: {.card-header}
🎒 Astuce Pro : Pourquoi le format colonnaire ?
:::
::: {.card-body}
En stockant les données d'une même colonne côte à côte en mémoire, le processeur peut utiliser des instructions **SIMD** (*Single Instruction Multiple Data*). Cela permet d'appliquer une opération sur un vecteur entier en un seul cycle d'horloge. C'est l'essence même de la performance en Data Science [@bato_2026_pyarrow_revolution].
:::
:::
### 🏎️ Eager vs Lazy : Deux philosophies de calcul

1. **Exécution Immédiate (Eager - Pandas) :** Chaque instruction est exécutée dès qu'elle est lue. Intuitif, mais génère souvent des copies inutiles en RAM.
2. **Exécution Différée (Lazy - Polars) :** Le moteur construit un plan d'optimisation (**Query Plan**) et ne calcule le résultat qu'au dernier moment, permettant d'ignorer les données inutiles dès la source.

---

#### 🧠 Simulateur : La Mémoire RAM en action

Observez comment le processeur accède aux données selon le type de stockage. Le format colonnaire est le secret de la rapidité des enquêteurs modernes.

```{ojs}
//| panel: input
// 1. Définition des contrôles interactifs
viewof storageMode = Inputs.radio(
  ["Orienté Ligne (ex: CSV, Pandas classique)", "Orienté Colonne (ex: Parquet, Polars, Arrow)"],
  { value: "Orienté Ligne (ex: CSV, Pandas classique)", label: "Structure en RAM :" }
)

viewof queryCol = Inputs.radio(
  ["Aucune", "Âge", "Salaire"],
  { value: "Aucune", label: "Simuler une requête d'analyse sur :" }
)

```

```{ojs}
//| echo: false
// 2. Définition des données et des couleurs
tableData = [
  { ID: "A1", Âge: 25, Salaire: "50k" },
  { ID: "B2", Âge: 30, Salaire: "60k" },
  { ID: "C3", Âge: 28, Salaire: "55k" }
]

colors = { ID: "var(--sol-blue)", Âge: "var(--sol-green)", Salaire: "var(--sol-orange)" }

// 3. Logique de rendu dynamique en HTML/CSS
html`
<style>
  .ram-grid { 
    display: flex; 
    flex-wrap: wrap; 
    gap: 8px; 
    padding: 20px; 
    background: var(--sol-base3); 
    border: 2px solid var(--sol-base2); 
    border-radius: 12px;
    align-items: center;
  }
  .ram-block { 
    padding: 12px 18px; 
    border-radius: 6px; 
    color: var(--sol-base3); 
    font-weight: 700; 
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    box-shadow: 0 2px 4px rgba(0, 43, 54, 0.1);
  }
  .ram-block.active { 
    transform: scale(1.1); 
    box-shadow: 0 10px 15px -3px rgba(0, 43, 54, 0.2); 
    z-index: 10;
  }
  .ram-block.dimmed { 
    opacity: 0.15; 
    filter: grayscale(80%);
  }
  .ram-pointer { 
    font-size: 20px; 
    color: var(--sol-base1); 
    font-weight: bold;
    margin: 0 5px;
  }
  .feedback-box {
    margin-top: 20px;
    padding: 15px 20px;
    border-radius: 8px;
    font-size: 1.1em;
    transition: all 0.3s ease;
  }
  .feedback-bad { background: rgba(220, 50, 47, 0.1); color: var(--sol-red); border-left: 5px solid var(--sol-red); }
  .feedback-good { background: rgba(133, 153, 0, 0.1); color: var(--sol-green); border-left: 5px solid var(--sol-green); }
  .feedback-neutral { background: var(--sol-base2); color: var(--sol-base01); border-left: 5px solid var(--sol-base1); }
</style>

<div>
  <div class="ram-grid">
    ${
      storageMode.includes("Ligne")
        ? tableData.map(row =>
            ["ID", "Âge", "Salaire"].map(col =>
              `<div class="ram-block ${queryCol === "Aucune" ? "" : queryCol === col ? "active" : "dimmed"}" style="background-color: ${colors[col]}">${row[col]}</div>`
            ).join("")
          ).join(`<div class="ram-pointer">→</div>`)
        : ["ID", "Âge", "Salaire"].map(col =>
            tableData.map(row =>
              `<div class="ram-block ${queryCol === "Aucune" ? "" : queryCol === col ? "active" : "dimmed"}" style="background-color: ${colors[col]}">${row[col]}</div>`
            ).join("")
          ).join(`<div class="ram-pointer">→</div>`)
    }
  </div>

  <div class="feedback-box ${
    queryCol === 'Aucune' ? 'feedback-neutral' : 
    storageMode.includes('Ligne') ? 'feedback-bad' : 'feedback-good'
  }">
    <strong>🔍 Analyse CPU :</strong><br>
    ${
      queryCol === "Aucune"
        ? "Sélectionnez une colonne ci-dessus pour observer le comportement du processeur lors de la lecture."
        : storageMode.includes("Ligne")
          ? `Pour lire '${queryCol}', le processeur doit scanner toute la mémoire et faire des sauts inutiles (<strong>Cache Misses</strong>). C'est inefficace.`
          : `Toutes les données '${queryCol}' sont côte à côte. Le processeur lit le bloc d'un seul coup (<strong>Cache Hits</strong>). C'est ultra-rapide !`
    }
  </div>
</div>
`

```

::: {.card .card-info}
::: {.card-header}
💡 Concept Clé : SIMD
:::
::: {.card-body}
Capacité d'un processeur à effectuer la même opération sur plusieurs données simultanément. Essentiel pour le traitement massif de données.
:::
:::
::: {.card .card-warning}
::: {.card-header}
⚠️ Danger : Copie de Données
:::
::: {.card-body}
Évitez de multiplier les copies de DataFrames intermédiaires. En mode "Eager", chaque copie consomme une portion précieuse de votre RAM.
:::
:::
Evaluation :** Stratégie consistant à attendre le dernier moment pour calculer un résultat, permettant d'optimiser l'ensemble des étapes de calcul.
